parameters:
- name: project
  type: string
- name: netFrameworkVersion
  type: string
- name: configuration
  type: string
- name: configPattern
  type: string
- name: workingDir
  type: string

steps:
- task: replacetokens@3
  displayName: 'Configure'
  inputs:
    targetFiles: '$(Pipeline.Workspace)/s/${{ parameters.workingDir }}/**/${{ parameters.configPattern }}'
    encoding: 'auto'
    writeBOM: true
    verbosity: 'detailed'
    actionOnMissing: 'warn'
    keepToken: false
    tokenPrefix: '#{'
    tokenSuffix: '}#'
    useLegacyPattern: false
    enableTelemetry: true

- task: DotNetCoreCLI@2
  displayName: 'Pack'
  inputs:
    command: publish
    publishWebProjects: True
    arguments: '--configuration ${{ parameters.configuration }} --framework ${{ parameters.netFrameworkVersion }} --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: True
    workingDirectory: ${{ parameters.workingDir }}